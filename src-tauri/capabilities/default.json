{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and verification windows",
  "windows": ["main", "verification"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
